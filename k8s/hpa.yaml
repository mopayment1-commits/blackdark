apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: blackdark-web
  namespace: blackdark
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: blackdark-web
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
