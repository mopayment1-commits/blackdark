# SonarCloud Automatic Analysis configuration (GitHub App).
# AA ignores sonar-project.properties — this file is the live config.
# Keep keys minimal (supported AA parameters only).

sonar.python.version=3.12
sonar.sourceEncoding=UTF-8
sonar.tests=tests
