# Production web + core intelligence (Railway-optimized, no optional heavy backends)
aiosqlite
aiohttp
pydantic
fastapi
uvicorn[standard]
jinja2
stripe
python-dotenv
vaderSentiment
textblob
ccxt

# Optional analytics / archive (lazy-imported; enable when volume attached)
pandas
pyarrow
