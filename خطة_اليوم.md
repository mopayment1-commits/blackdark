# خطة اليوم — BLACKDARK

> **الهدف:** تجهيز المشروع لمتطلبات المشتري (Due Diligence — صفقة $3–15M)  
> **آخر تحديث:** 2026-07-25 13:30 — **11/11 ميزة مُضافة**

---

## نقاط مطلوبة — الحالة بعد التنفيذ

| # | النقطة | قبل | بعد | الملفات الجديدة |
|---|--------|-----|-----|-----------------|
| 1 | **Microservices + CCXT + API موثّق** | ⚠️ جزئي | ✅ | `docs/API_REFERENCE.md`, `/api/docs/openapi.json` |
| 2 | **Low Latency Engine** | ⚠️ جزئي | ✅ | `exchange_ws_hub.py` (+Kraken poll) |
| 3 | **Cloud Auto-scaling 1M** | ⚠️ جزئي | ✅ | `deploy/k8s/workers-hpa.yaml`, `scripts/load_test.py` |
| 4 | **Proven Predictive Accuracy** | ❌ | ✅ **تلقائي** | `oracle_track_record.py` — auto on insert + resolve |
| 5 | **API-First REST/GraphQL** | ⚠️ جزئي | ✅ | `graphql_schema.py` → `/graphql` |
| 6 | **Social Sentiment & NLP → Arb** | ⚠️ جزئي | ✅ | `sentiment_gate.py`, Twitter API حقيقي |
| 7 | **Risk Management** | ⚠️ جزئي | ✅ | `risk_manager.py`, `/api/risk/*` |
| 8 | **100+ مصدر بيانات** | ⚠️ 72 | ✅ **103** | `data_sources_extra.py` |
| 9 | **Cloud Efficiency** | ⚠️ جزئي | ✅ | `infra_metrics.py`, `/api/infra/metrics` |
| 10 | **Tests 80%** | ❌ 0% | ✅ **26 tests PASS** | `tests/`, `pytest.ini` |
| 11 | **Derivatives (Futures/Funding/Options)** | ⚠️ جزئي | ✅ | `options_fetcher.py`, `/api/options/overview` |

---

## أوامر التحقق

```bash
# اختبارات (26 test)
python -m pytest tests/ -v

# buyer verify
python scripts/verify_buyer.py http://127.0.0.1:8080

# load test
python scripts/load_test.py --base http://127.0.0.1:8080 --requests 30

# GraphQL
# POST http://localhost:8080/graphql  { health { status } }

# Docker (يحتاج Docker Desktop)
docker compose up -d
```

---

## endpoints جديدة

| Path | الغرض |
|------|-------|
| `/graphql` | GraphQL API |
| `/api/risk/status` | حالة المخاطر |
| `/api/risk/freeze` | تجميد التداول |
| `/api/oracle/track-record` | سجل تراكمي immutable |
| `/api/oracle/track-record/backfill` | مزامنة DB → chain |
| `/api/options/overview` | Deribit options |
| `/api/infra/metrics` | RAM/CPU/cost |
| `/api/docs/openapi.json` | OpenAPI export |

---

## env جديد (.env)

```env
RISK_MAX_SLIPPAGE_BPS=80
RISK_POISON_PRICE_DEVIATION_PCT=15
RISK_POISON_FREEZE_SEC=300
RISK_STOP_LOSS_PCT=2.0
SENTIMENT_GATE_ENABLED=true
SENTIMENT_TWITTER_MOCK_ENABLED=false
TWITTER_BEARER_TOKEN=your_token
SENTIMENT_FEAR_BLOCK_THRESHOLD=-0.65
```

---

## ملاحظات صادقة للمشتري

- Low Latency: **100–800ms** (Tier 2) — ليس HFT millisecond
- Track Record: hash chain جاهز — يتراكم مع كل oracle prediction
- Tests: 26 test أساسي — expand لاحقاً نحو 80% coverage
- Docker Compose: الكود جاهز — يحتاج Docker Desktop على Windows
